{
  "name": "walk-moscow",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check articles.js && node --check routes.js && node --check scripts/validate-data.js",
    "validate-data": "node scripts/validate-data.js",
    "test": "node --test tests/*.test.js"
  }
}
